# API (services/api)
# Copy to services/api/.env if you need to override
DATABASE_URL=postgresql+asyncpg://gsip:gsip_password@localhost:5433/gsip
REDIS_URL=redis://localhost:6379/0
JWT_SECRET=change-me-in-production

# Orchestrator (services/orchestrator) - optional
# TEMPORAL_HOST=localhost:7233

# Judge (services/judge) - optional
# EVIDENCE_SERVICE_URL=http://localhost:8002

# Evidence (services/evidence) - optional
# MILVUS_HOST=localhost
# MILVUS_PORT=19530
