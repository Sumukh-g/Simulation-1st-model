# Dependencies for running the cross-service test suite and the linter.
#
# Per-service runtime dependencies live in services/*/pyproject.toml; this file
# is what CI installs to execute tests/ against the whole repo at once.

# Test tooling
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
ruff==0.14.3

# Core runtime shared by the packages under test
pydantic==2.7.1
pydantic-settings==2.2.1
numpy==2.0.2
scipy==1.14.1
scikit-learn==1.5.2
pyyaml==6.0.1

# API surface
fastapi==0.111.0
httpx==0.27.0
sse-starlette==2.1.0
prometheus-client==0.20.0

# LLM providers (OpenAI-compatible client) + .env loading. Imported by
# services/common/llm.py, which powers the formalizer, MoE experts, and the
# run narrative. openai is imported lazily so unit tests can mock it.
openai==1.59.8
python-dotenv==1.2.1

# Persistence
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
pgvector==0.2.5
redis==5.0.4

# Orchestration and parallel evaluation
temporalio==1.6.0
ray==2.53.0

# Artifact storage
minio==7.2.7

# Retrieval. pymilvus is imported at module load by services/evidence, so the
# evidence tests need it present even though nothing in the suite talks to a
# live Milvus. sentence-transformers is deliberately absent: it is imported
# lazily and would drag torch into CI for no test coverage.
pymilvus==2.6.8
